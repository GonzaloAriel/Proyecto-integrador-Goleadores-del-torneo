/*if (mes == 2)    // Si es febrero
{
    if ((anio % 4 == 0 && anio % 100 != 0)  (anio % 400 == 0))    // Año bisiesto
    {
        if (d < 1  d > 29)
        {
            throw std::invalid_argument("Error: En año bisiesto, febrero tiene hasta 29 días.");
        }
    }
    else      // Año no bisiesto
    {
        if (d < 1  d > 28)
        {
            throw std::invalid_argument("Error: En año normal, febrero tiene hasta 28 días.");
        }
    }
}
else if (mes == 4  mes == 6  mes == 9  mes == 11)      // Meses con 30 días
{
    if (d < 1  d > 30)
    {
        throw std::invalid_argument("Error: Este mes solo tiene 30 días.");
    }
}
else      // Meses con 31 días
{
    if (d < 1  d > 31)
    {
        throw std::invalid_argument("Error: Este mes tiene hasta 31 días.");
    }
}
*/
